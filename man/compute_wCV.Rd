% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_QC.R
\name{compute_wCV}
\alias{compute_wCV}
\title{Core helper to compute a window coefficient of variation}
\usage{
compute_wCV(
  pileupData,
  sampleInfo,
  rnum = 100,
  method = 1,
  winSize = 20,
  egPct = 10,
  cases = NULL
)
}
\arguments{
\item{pileupData}{exon-only coverage pileup matrix for a single gene.}

\item{sampleInfo}{a sample information table including sample id. The number of rows is equal to the number of samples.}

\item{rnum}{the number of regions for uniformly dividing the x-axis. Default is 100.}

\item{method}{1 and 2 return the raw read depth and the interpolated read depth at the normalized genomic position, respectively. Default is 1.}

\item{winSize}{window size of the rolling window. Default is 20.}

\item{egPct}{edge percent (one-side) to calculate the trimmed mean. Default is 10.}

\item{cases}{optional character vector specifying a subset of samples.
used for handling missing coverage.}
}
\value{
a numeric vector of window coefficients of variation, one value per sample.
}
\description{
Core helper to compute a window coefficient of variation
}
\examples{
## API illustration only
## Exon-only pileup matrix (rows: exon positions, columns: samples)
## Typically obtained via get_pileupExon()
pileupData <- matrix(
  c(10, 12, 8,  9,
    5,  6,  4,  5),
  nrow=2,
  byrow=TRUE
)
colnames(pileupData) <- c("S1", "S2", "S3", "S4")

sampleInfo <- data.frame(SampleID=colnames(pileupData))

compute_wCV(
  pileupData = pileupData,
  sampleInfo = sampleInfo,
  rnum       = 10,
  winSize    = 2
)
}
